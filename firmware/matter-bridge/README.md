# matter-bridge

ESP-IDF + esp-matter project for the Seeed XIAO ESP32C6: joins Thread
(HomePod mini as Border Router) and bridges timer / med-status / house
temp setpoint / heat-cool / scene-trigger endpoints into Apple Home.
Talks to `display-panel` over UART.
